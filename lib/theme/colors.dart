import 'package:flutter/widgets.dart';

/// A class containing the color constants used in the app.
abstract class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF1B31A8);
  static const Color secondary = Color(0xFF1E2A32);
  static const Color background = Color(0xFFFFFFFF);
  static const Color backgroundSecondary = Color(0xFFF4F8FA);
  static const Color accent = Color(0xFF4D6475);
  static const Color info = Color(0xFF708797);
  static const Color buttonPrimary = Color(0xFF001C95);
}
